const styles = theme => ({
  coloredIcon: {
    color: theme.palette.common.blue,
    padding: '0px 10px'
  },
  mibarrio_footer: {
    backgroundColor: theme.palette.primary.main,
    minHeight: '35px'
  }
})

export default styles
